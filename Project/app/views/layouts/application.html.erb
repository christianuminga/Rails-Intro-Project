<!DOCTYPE html>
<html>
  <head>
    <title>Project</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="stylesheet" href="https://bootswatch.com/4/cyborg/bootstrap.min.css">
    </head>
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="<%= root_path%>">311 Requests</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item active">
          <a class="nav-link" href="<%= root_path%>">Home <span class="sr-only">(current)</span></a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="<%= all_requests_path %>">All Requests</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="<%= about_path%>">About</a>
        </li>
      </ul>
      <%= form_tag(results_search_index_path, method: "get") do %>
        <%= select_tag"category", options_from_collection_for_select(ServiceArea.all, "id", "name"), :include_blank => "All" %>
        <%= text_field_tag(:q) %>
        <%= submit_tag("Search") %>
      <% end %>
    </div>
  </nav>
<%= yield %>


        <footer>
      <p> Full-Stack Project 2018 </p>
    </footer>
</html>
